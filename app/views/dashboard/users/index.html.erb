<p id="notice"><%= notice %></p>

<h1>Dashboard Users</h1>

<table>
  <thead>
    <tr>
      <th>Email</th>
      <th>Password</th>
      <th>Password confirmation</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.email %></td>
        <td><%= user.password %></td>
        <td><%= user.password_confirmation %></td>
        <td><%= link_to 'Show', dashboard_user %></td>
        <td><%= link_to 'Edit', edit_dashboard_user_path(dashboard_user) %></td>
        <td><%= link_to 'Destroy', dashboard_user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Dashboard User', new_dashboard_user_path %>
